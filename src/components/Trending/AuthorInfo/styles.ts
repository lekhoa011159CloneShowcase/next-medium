import { SxProps } from "@mui/material";

const sx = {
  container: { mb: 1, display: "flex", alignItems: "center" } satisfies SxProps,

  avatar: { width: 24, height: 24, mr: 1 } satisfies SxProps,
};

export default sx;
